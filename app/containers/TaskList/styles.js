const styles = (theme) => {
  return {
    mainContainer: {
      flex: 1,
    },

    leftSwipeContainer: {
      flex: 1,
      backgroundColor: theme.colors.green,
    },

    rightSwipeContainer: {
      flex: 1,
      backgroundColor: theme.colors.red,
    },
  }
}

export default styles
