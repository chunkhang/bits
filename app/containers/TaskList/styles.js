const styles = (theme) => {
  return {
    leftSwipeContainer: {
      flex: 1,
      backgroundColor: theme.colors.green,
    },

    rightSwipeContainer: {
      flex: 1,
      backgroundColor: theme.colors.red,
    },
  }
}

export default styles
