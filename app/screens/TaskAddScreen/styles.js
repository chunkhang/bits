const styles = (theme) => {
  return {
    mainContainer: {
      position: 'absolute',
      bottom: 0,
      width: '100%',
      backgroundColor: theme.colors.white,
    },

    body: {
      paddingHorizontal: 24,
      paddingVertical: 12,
    },
  }
}

export default styles
