const styles = (theme) => {
  return {
    mainContainer: {
      position: 'absolute',
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      backgroundColor: theme.colors.whiteTranslucent,
    },
  }
}

export default styles
