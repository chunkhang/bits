const styles = (theme) => {
  return {
    mainContainer: {
      justifyContent: 'center',
    },

    stroke: {
      position: 'absolute',
      alignSelf: 'center',
      backgroundColor: theme.dynamics.background,
      borderRadius: 2,
    },
  }
}

export default styles
