const styles = (theme) => {
  return {
    mainContainer: {
      justifyContent: 'center',
    },

    stroke: {
      position: 'absolute',
      alignSelf: 'center',
      backgroundColor: theme.colors.white,
      borderRadius: 2,
    },
  }
}

export default styles
